global:
  scrape_interval: 15s      # Ambil metrics tiap 15 detik

scrape_configs:
  - job_name: 'skin-disease-detector'
    static_configs:
      - targets: ['host.docker.internal:8080']    # Alamat Flask app kita
    metrics_path: '/metrics'                       # Endpoint metrics Flask